import express from 'express';
// import { createData, getData, getOneData, updateData, deleteData } from '../controllers/dataController.js';
// import { verifyToken } from "../middleware/auth.js";

const router = express.Router();

// router.get("/get-data/:route_id", verifyToken, getData);
// router.get("/get-one-data/:id", verifyToken, getOneData);
// router.post("/create-data", verifyToken, createData);
// router.put("/update-data/:id", verifyToken, updateData);
// router.delete("/delete-data/:id", verifyToken, deleteData);


export default router;
